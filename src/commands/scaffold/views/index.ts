import { ScaffoldSchema } from "../../../types.js";

export const createViewsAndComponents = (schema: ScaffoldSchema) => {
  const { tableName, fields } = schema;

  // generate routes

  // generate components
};
